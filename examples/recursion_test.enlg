# EnLang Recursive Function Example
# Recursively prints numbers from n to 10

function print_numbers(n):
    if n>10 then:
        return
    display n
    print_numbers(n+1)
     print_numbers(2)


